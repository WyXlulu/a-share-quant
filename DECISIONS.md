# 决策日志 (DECISIONS.md)

> **只追加，永不删改旧条目。** 推翻旧决策时新写一条"取代 X"。
> 格式：日期 | 决定 | 理由 | 放弃的备选。

---

## 2026-06-29 | 用 git + GitHub 做版本管理

- **决定**：代码与三份活文档统一进单一 git 仓库，GitHub 私有托管。
- **理由**：状态落盘、可回退、对抗对话上下文上限。
- **备选**：仅本地（无备份，放弃）；Gitee（国内访问更稳，保留为镜像备份候选）。

---

## 2026-06-29 | 复权口径定为等比复权

- **决定**：研究与实盘全链路统一用等比复权。
- **理由**：与 QMT 实盘行情口径一致，避免回测/实盘 mismatch。
- **备选**：等差复权（同花顺口径）——与执行层不一致，放弃。

---

## 2026-06-29 | 采用三份活文档机制（PROJECT / STATE / DECISIONS）

- **决定**：按信息的"变化速度"拆成三份文档：PROJECT（慢，原地改）、STATE（快，可覆盖）、DECISIONS（只增不减）。
- **理由**：三类信息生命周期不同，混在一份会互相污染；分开后各自按节奏演化。让任意失忆的新会话能快速重新进入项目——这是"对话可丢弃、仓库永久"得以成立的根基。DECISIONS 是 ADR（架构决策记录）的简化版。
- **备选**：单一大文档（高频变动会搅乱稳定内容，放弃）。

---

## 2026-06-29 | 单一 coding agent 主力 = Claude Code

- **决定**：代码工作以 Claude Code 为主力，不让两个 agent 同时改同一仓库。
- **理由**：避免多 agent 协调成本与冲突。
- **备选**：Codex（保留为临时第二意见，不并行改 repo）。

---

## 2026-06-29 | 策略频率先锁定中低频

- **决定**：起步只做中低频（日频~分钟频）策略。
- **理由**：数据自取得到、不触发高频监管（300 笔/秒或 2 万笔/日）、不被 L2 数据卡住。
- **备选**：盘口/高频（数据贵且部分锁在券商，后置）。

---

## 2026-06-29 | Claude Code 指令必须明确

- **决定**：全程对 Claude Code 下达的所有指令必须明确，不留歧义——说清(1)具体动作、(2)涉及的文件/范围、(3)需遵守的约定、(4)可验证的完成标准。
- **理由**：模糊指令是 agent 出错和自作主张的主要来源；明确指令让结果可预期、可检查。
- **适用范围**：写代码、改文档、git 操作、跑测试等一切指令。
- **备选**：依赖 agent 自行推断（易跑偏，放弃）。

---

## 2026-06-30 | 原始数据保持不可变只读

- **决定**：原始数据 `data/raw/` 保持不可变只读；所有清洗与过滤都在下游派生层用显式规则处理。
- **理由**：沪深300十年日线数据质检确认红旗均为真实市场事件（停牌复牌、科创板/注册制新股首日无涨跌幅限制）。直接删除这些记录会篡改真实收益、制造回测偏差，因此保留原始数据，并在回测层按规则应对。
- **备选**：在原始层删除或改写异常记录（会破坏数据真实性，放弃）。

---

## 2026-06-30 | 回测设计规范冻结为 V3.0.2

- **决定**：回测设计规范 `docs/BACKTEST_DESIGN.md` 冻结为 V3.0.2，作为当前唯一强制实施基准；后续迭代只改内容，不改文件名 `BACKTEST_DESIGN.md`。
- **理由**：固定规范入口，避免多版本文件名造成开发者和 agent 引用漂移；V3.0.2 已作为冻结实施基准进入仓库。
- **备选**：按版本号不断新增规范文件（容易产生入口混乱，放弃）。

---

## 2026-06-30 | 数据 parquet 不入 git

- **决定**：`data/` 下的 parquet 数据文件不提交进 git；仓库只保留 build 脚本、manifest 和必要元数据，数据由脚本与 manifest 复现。
- **理由**：git 不擅长管理二进制大文件，parquet 数据会快速膨胀仓库体积；当前 L1 与隔离数据都是可由数据源、脚本、参数和 manifest 确定性派生的产物。
- **备选**：直接提交 parquet（短期方便但长期污染仓库，放弃）。

---

## 2026-06-30 | akshare 数据源采用多 provider fallback

- **决定**：日线数据接入通过 `src/data/akshare_adapter.py` 统一封装 akshare 多 provider fallback；实测本次 L1 重建中新浪为主通道（297/300），东财补充科创板 3 只。
- **理由**：本地代理与单一数据源接口都可能不稳定；adapter 边界内直连、fallback 与标准英文 schema 能提高可重复构建成功率，并阻止中文源字段和 provider 细节泄漏到下游。
- **备选**：只依赖东财接口或在业务层散落 provider 调用（不稳定且污染边界，放弃）。

---

## 2026-06-30 | 当前 ST / 状态使用真实快照但保留污染标记

- **决定**：当前 ST / 状态已通过真实接口 `stock_info_a_code_name` 的当前快照取得真值，沪深300 当前 ST 数为 0，已确认不是占位值；但该数据仍只标记为 `CURRENT_SNAPSHOT_ONLY`，并保留 `EXPLORATORY_TAINTED`。
- **理由**：当前快照只能回答“现在是否 ST / 退市”，不能回答历史任一 `asof_ts` 的 ST / 状态。用于可信回测的历史时点判断仍需要 Tushare 等具备时点能力的数据源补齐。
- **备选**：把当前快照当作历史状态使用（未来函数风险，放弃）；在没有真实快照时静默占位（不可审计，放弃）。

---

## 2026-06-30 | PITDataPortal 作为唯一 PIT 只读访问层

- **决定**：PITDataPortal 是上层访问 PIT 数据的唯一只读入口；对缺少 `available_at` 的表一律 fail-closed，并按字段级 `available_at` 做 as-of 过滤：稳定属性按上市日可见，`CURRENT_SNAPSHOT_ONLY` 字段按快照拉取日可见。
- **理由**：Portal 是整个防泄露架构的物理闸门，保证任何 `available_at > asof_ts` 的数据不会流向特征、策略或执行层；字段级可见性让 security master 这类宽表既保留稳定属性，又阻断当前快照字段穿越历史。
- **备选**：上层直接读取 parquet 或把缺时间戳字段默认视为可见（会绕过防泄露闸门，放弃）。

---

## 2026-07-01 | ST 5% 涨跌幅限幅暂缓实现

- **决定**：涨跌停开盘拒单暂只按 `security_master.board` 的板块限幅处理（主板 10% / 创业板与科创板 20% / 北交所 30%），暂不实现 ST 股 5% 限幅。
- **理由**：当前 `security_master.is_st` 是 `CURRENT_SNAPSHOT_ONLY` 污染字段，不可用于历史时点判定；沪深300当前 ST = 0，历史个别 ST 段会被按板块宽松限幅处理，属于已知有界小偏差。待接入具备时点能力的 ST 数据（如 Tushare）后，再补上 ST 5% 限幅。
- **备选**：直接用当前快照 `is_st` 判断历史 ST（未来函数风险，放弃）；在没有 PIT ST 数据前硬编码 ST 区间（不可审计，放弃）。

---

## 2026-07-01 | L2 公司行动台账为 best-effort

- **决定**：L2 公司行动台账先以 best-effort 形式进入 L2，整体标记 `EXPLORATORY_TAINTED`；下一步账本逻辑不处理未识别或未支持类型，遇到时以 `UNSUPPORTED_CORPORATE_EVENT` 阻断。
- **理由**：当前 akshare 台账已覆盖沪深300中 295/300 只股票、3075 条记录，可支撑分红/送转账本的第一版实现，但完整性和精确 PIT 可得时点未经逐 provider 人工核验。已知缺口包括：`SPLIT` 未被接口独立识别（A 股拆股罕见，多为送转并已归入 `STOCK_DIVIDEND`）；`MERGER` / `DELIST` 未覆盖；`RIGHTS_ISSUE` 仅通过 Sina 配股接口 best-effort；5 只次新股（001280 / 688047 / 688506 / 688521 / 688981）无记录；精确 `available_at` 披露时点未逐 provider 核验。
- **备选**：等待 Tushare 等更完整数据源再实现账本（会阻塞主线，放弃）；把未覆盖类型静默当作无事件（会污染回测账本，放弃）。后续接入更完整数据源（如 Tushare）后补齐缺口。

---

## 2026-07-01 | 分红对账口径:以台账原始凭证为准,废弃raw-hfq收益差反推法

- **决定**：现金分红对账以 L2 公司行动台账的原始文本凭证为准；`cash_dividend_per_share` 采用数据源原始分派文本换算后的税前每股金额。后续现金账本按税前每股分红记应收，红利税建模另行决策。
- **理由**：600519 案例中，数据源原始文本为 `10派280.2423元(含税)`，经核查换算为每股税前分红 `28.02423` 元，换算正确、无同日分派漏合并、口径为税前，可信采用。此前曾用 raw 与 hfq 当日收益之差估算除息缺口（约 2.56%）并反推每股分红（约 31 元），该方法混入了当日真实市场波动与供应商复权因子的乘性/加性口径差异，不精确，予以废弃。今后校验分红统一用加性口径：除息参考价 = 前收盘 - 每股税前分红，实际价格相对参考价的偏离视为市场波动。
- **案例**：600519 在 2026-06-26 前收盘 `1212.10`，当日收盘 `1168.63`，共跌 `43.47`；其中 `28.02` 为除息，约 `15.45` 为真实市场下跌。
- **备选**：继续用 raw-hfq 收益差反推分红（不稳定且会混入口径差和市场波动，放弃）。

---

## 2026-07-01 | 印花税归零 bug 的发现与修复

- **决定**：`FeeSchedule` 的制度性费用必须覆盖已知历史生效日，并在请求日期早于最早已知费率时 fail-closed；印花税规则已修为 2008-09-19 起卖出单边 `0.1%`，2023-08-28 起卖出单边 `0.05%`，早于 2008-09-19 的日期抛 `FeeScheduleError`。
- **理由**：审计发现 `_resolve_rate` 曾在无生效费率时静默返回 0，导致 2015 至 2023-08-27 的卖出印花税被算为 0；同时旧测试曾断言 2023-08-27 税率为 0，把错误固化成绿灯。该问题会系统性低估历史卖出交易成本，必须显式修复并禁止静默归零。
- **备选**：继续默认无生效费率为 0（会隐藏制度性费用缺口，放弃）；用伪造的 1900-01-01 起点覆盖未知历史（掩盖数据边界，放弃）。

---

## 2026-07-01 | 制度性数字测试断言必须来自外部事实源

- **决定**：制度性数字（费率、涨跌幅、实施日期、首日限价规则等）的测试断言必须来自外部事实源，如法规、公告、交易所规则或已确认的版本表；不得从当前实现反推。
- **理由**：顺着实现写断言会把 bug 固化为绿灯。本次印花税测试曾断言 2023-08-27 税率为 0，正是实现错误反向污染测试的例子。后续凡涉及制度性数字，测试应先写明外部事实口径，再验证实现是否符合。
- **备选**：以当前代码行为作为测试预期（无法发现规则实现错误，放弃）。

---

## 2026-07-01 | 审计修复包一:工程修复

- **发现**：`src/calendar` 与 Python 标准库 `calendar` 同名，导致 `unittest discover` 在部分环境下导入漂移或失效；依靠手工模块列表运行测试，存在新增测试静默漏跑风险。数据依赖测试在无本地 parquet/manifest 数据的新机器上会 error，而不是报告显式 skip。
- **原因**：项目包名占用了标准库名称；测试入口没有统一自动发现机制；真实数据文件未入 git，数据契约测试缺少缺文件前置检查。
- **修复**：用 `git mv` 将 `src/calendar` 改名为 `src/market_calendar`，并更新全项目 import；为 `test_l1_raw`、`test_security_master`、`test_corporate_actions`、`test_quarantine_vendor_adjusted` 的真实数据依赖用例增加缺文件显式 skip；新增 `run_tests.py`，通过 `unittest discover` 自动发现 `src` 下所有 `test_*.py`；补充附注标签 `phase0-complete` 与 `spec-frozen`。
- **验证**：`.venv\Scripts\python.exe run_tests.py` discover 发现 50 个测试，与原显式测试函数数 50 一致；当前有数据环境下 `passed=50, skipped=0, failures=0, errors=0`；全项目检查无旧 calendar import 路径残留。

---

## 2026-07-01 | 审计修复包二:语义修复

- **发现**：CA 台账 `available_at` 曾等于 `ex_date`，会使除权日参考价修复被自身 PIT 闸门阻塞；取证显示 `announcement_date` 100% 非空，领先除权日 0-17 天。限价检查曾 fail-open，缺前收盘、缺 security master 或 T 日停牌时会静默跳过限价判断。EX-005 lot size 曾无执行引擎级强制。
- **原因**：公司行动台账没有分离事件发生时间与可知时间；执行层把“无法构造限价上下文”和“无需限价拒单”都折叠成 `None`；订单数量只在 dummy strategy 层校验，executor 没有独立防线。
- **修复**：CA 台账改为 `event_ts=ex_date 15:00 Asia/Shanghai`、`available_at=announcement_date 15:00 Asia/Shanghai`，公告日缺失时 fallback 到 ex-date 并计数入 manifest；限价前收盘改为通过同一 PIT Portal 在 T 日 as-of 下向前回看 60 个交易日最近非空 close，并在 `FillLedgerEntry.limit_check` 记录 `APPLIED`、各类 `SKIPPED_*` 或 `EXEMPT_NEW_LISTING`；买单 lot size 在 `T1OpenExecutor` 成交前强制，默认 `ROUND_DOWN` 到 100 股整数倍，可配 `REJECT`，并用 `requested_quantity` 留存原始订单数量。卖单零股规则留待持仓账本步完整处理。
- **验证**：CA manifest 记录 `announcement_date_non_null_count=3075`、fallback=0、领先区间 0-17 天，唯一 0 天差证券为 `601318`；CA Portal 测试已验证公告日 15:00 后、除权日前可见。限价四类测试覆盖停牌复牌向前取前收盘、无前收盘、无 master、正常 `APPLIED`；lot size 三类测试覆盖默认向下取整、`REJECT` 策略、整手不变。`.venv\Scripts\python.exe run_tests.py` 全量 `total=58, passed=58, skipped=0, failures=0, errors=0`。
- **后续**：除权日限价参考价仍有 TODO，需在 CA 账本步接入公司行动调整后的参考价；本次 CA `available_at` 修复是该工作的前置解锁。

## 2026-07-02 | EX-010 同轮次现金交互采用显式 broker adapter 规则

- **决定**：同一 T+1 开盘轮次，broker adapter 执行顺序显式固定为先处理全部卖单、账本入账后再处理全部买单；卖出 `FILLED` 所释放的净额现金计入本轮买单可用现金。实盘对接时须逐字核对券商真实资金可用规则。
- **保守选择**：T 日买单锁定阶段不预支未来卖出资金；现金可行性只看 T 日已有可用现金与既有冻结预留逻辑。即使同一决策日存在卖出意图，现金不足的买单也在 T 日锁定阶段返回 `CASH_INSUFFICIENT`，防止 T+1 卖单被拒、停牌或缺开盘价时买单超支。
- **理由**：执行适配层需要模拟同一开盘批次内卖出成交后的现金释放，但锁定层必须保持 fail-closed，不能把尚未发生且可能失败的卖出视作确定现金。
- **备选**：T 日锁定阶段允许用同日卖出意图预支现金（会把未来不确定成交前移成现金承诺，放弃）。
---

## 2026-07-02 | CA账本的两项声明式简化

- **派息到账日**：公司行动台账当前无派息日字段，CA账本采用“除息日后第1个交易日到账”的显式简化。实际到账日因公司而异，通常在数日内；待接入更完整数据源（如 Tushare 分红表含派息日）后修正。
- **红利税**：CA账本按税前口径记应收，暂不建模持有期差别税率（10% / 20% / 免征）。实盘现金流会低于当前回测记账，后续接入红利税模型后修正。
- **已知有界局限**：`601318` 那条公告日等于除权日的公司行动，在除权日日切时因 `available_at=当日15:00` 不可见，按 `UNPROCESSED_CA` fail-closed 处理，不静默放行。
---

## 2026-07-02 | runner快路径缓存portal

- **决定**：十年端到端回测 runner 在组装层引入 `CachedPITDataPortal`，对 parquet 表做预解析与内存缓存，并对日线按证券建立索引；同时用 runner 专用的 `RunnerDummyRebalanceStrategy` 预取“当日可交易池”。核心 `PITDataPortal`、执行引擎、账本和公司行动处理逻辑不改。
- **原因**：原始 runner 逐日、逐证券反复通过慢路径读取并解析全表，完整十年回测曾在 10 分钟内无法完成。缓存层只服务 runner 的性能目标，不改变 PIT 语义边界。
- **等价性哨兵**：永久测试已覆盖快慢路径一致性：同一三个月 fixture 同时跑原始 `PITDataPortal` 与 `CachedPITDataPortal`，断言 `LockedOrder` 序列、`FillLedgerEntry` 序列、账本事件流、`nav_daily` 字节完全一致；LT-002 Phase 1 未来突变测试也直接压在缓存路径上。
- **防未来泄露约束**：缓存 portal 每次 query 仍按 `available_at <= asof` 截断，日线额外按 `event_ts <= asof` 截断；预取可交易池时逐行按自身 `trade_date 15:00 +08:00` 可见性过滤，再按 `trade_status == 正常` 生成当日池。
- **备选**：继续使用原始 portal 跑十年回测（语义简单但不可接受地慢，放弃）；把缓存逻辑下沉到核心 `PITDataPortal`（影响面更大，Phase 1 收官阶段放弃）。

---

## 2026-07-02 | 费用口径注记

- **决定**：Phase 1 账本中交易费用以净额进入买入成本、卖出净收入，并由此进入 realized / unrealized PnL；端到端 runner 的 `run_manifest.json` 另列 `total_fees` 作为统计摘要。
- **会计恒等式口径**：每日净值恒等式按该净额口径闭合：期末净值 = 期初净值 + 已实现PnL + 未实现PnL变动 + 分红应收新增。费用不在恒等式中再次单独扣减，避免双扣。
- **原因**：当前 `PortfolioLedger` 的事实源是净额现金流与 FIFO cost basis；在 Phase 1 不额外维护毛额权益曲线。费用明细保留在 `FillLedgerEntry`，汇总写入 manifest，后续可在报表层展开。
- **备选**：将费用从 PnL 中拆出并在权益公式里单列（更适合后续分析报表，但需要账本口径升级，Phase 1 暂不做）。

---

## 2026-07-02 | amount单位校验结论

- **决定**：L1 `daily_bar_raw.amount` 口径确认为人民币元，`volume` 口径确认为股；EX-008 容量约束的 trailing ADV 计算无需做千元换算或手数换算。
- **取证**：跨板块三票数量级校验，计算 `amount / (((open + close) / 2) * volume)`：`600519` 为 `1.0047`，`300750` 为 `1.0153`，`688981` 为 `0.9908`。比值约为 1，支持 `volume=股` 且 `amount=元`；若 `volume=手` 比值会约为 100，若 `amount=千元` 比值会约为 0.001。
- **永久哨兵**：新增 `src/data/test_l1_amount_units.py`，固定抽样 `600519` / `300750` / `688981`，断言上述数量级比值落在 `0.85~1.15`，防止未来数据源字段单位悄悄变化。
- **备选**：把 `amount` 视作千元并在容量约束中乘 1000（与实测数量级矛盾，放弃）。

---

## 2026-07-02 | 声称必须可复核案例

- **事件**：曾在思考中声称“成交额为千元并已换算”。复查确认代码从未包含该换算，且当时没有可复核判定依据。
- **校正**：随后以三只跨板块股票的 `amount / (((open + close) / 2) * volume)` 数量级实测一锤定音，确认数据一直是元口径，并将结论固化为永久测试。
- **纪律**：任何数据口径、制度数字、交易规则或单位换算断言，必须附可复核依据；没有依据时只能标记为待确认，不得把推测写成事实。
- **适用范围**：数据源字段单位、成交额/成交量口径、费用制度、涨跌停规则、公司行动口径等所有会影响回测结果的基础事实。

---

## 2026-07-02 | Gate部署与券商档位延后决策

- **决定**：Phase 1 毕业后，研究层部署固定为本机；券商档位与执行层 Windows / miniQMT 部署暂缓至上线前再定。
- **理由**：Phase 2 的主线仍是 PIT 复权服务、截面动量与黄金切片验证，不依赖券商资金门槛或常开执行环境。过早绑定券商档位和 Windows 部署会增加维护成本，却不能提高当前研究与回测链路可信度。
- **备选**：现在即锁定券商档位并搭建执行层 Windows 环境（对 Phase 2 无直接收益，放弃）；继续在 PROJECT 中保留“待确认”（已不符合 Phase 1 毕业后的项目事实，放弃）。

---

## 2026-07-02 | 终态蓝图入档

- **决定**：新增 `docs/PRODUCTION_BLUEPRINT.md`，将 Phase 4/5 终态生产蓝图入档：系统定位为日频，每日一次决策，盘中仅监控、对账、告警与 kill switch；终态保留人在回路的每日订单审批，确认后的 `LockedOrder` 才允许进入 broker adapter。
- **架构边界**：Phase 2/3 必须守住三条不变边界：`LockedOrder` 与 `FillLedgerEntry` 是回测和实盘共同语言；信号函数回测与实盘同一份代码、只换数据源；账本事件流不可变、可 diff。策略层永远只产 `OrderIntent`，不得绕过锁定与执行边界。
- **理由**：提前固定生产形态和边界，可以让 PIT 复权服务、特征、策略、账本持久化与未来 miniQMT adapter 沿同一接口成长，避免 Phase 2/3 写出只适合研究、不适合实盘对账的捷径。
- **备选**：盘中实时交易暂不做；个人无速度优势，执行模拟与基础设施代价数量级上升，且触及高频监管认定。该方向留待日频验证出真实 alpha 后再评估。

---
## 2026-07-03 | 共享参考价规则模块固化 + execution.py 迁移
- **发现**：execution.py 除权日限价参考价(7b9c651)公式为 `(prev_close - D)/(1+s)`，缺配股项；官方《上海证券交易所/深圳证券交易所交易规则(2023修订)》(上交所4.3.2 / 深交所4.4.2,检索2026-07-03)为 `[(前收盘-现金红利)+配股价×股份变动比例]/(1+股份变动比例)`。
- **影响**：纯配股除权日现有实现参考价偏差约 `r·((P-D)-配股价)/(1+r)`；配股价通常低于除息后前收盘，故参考价偏高、涨跌停价偏高(偏乐观方向)。有界：L2台账`RIGHTS_ISSUE`仅19条，持仓遇配股已被EX-013 taint，Phase 1哑策略几乎不触及配股标的持仓。
- **修复**：新建 `src/domain/corporate_action_pricing.py` 固化完整官方公式(含配股项)，按生效日2023-02-17版本化、早于此fail-closed；测试期望值全部取官方公式手算(含 D/s/r 三者非零组合守门算例，验证分母为`1+s+r`而非`1+s`或`1+r`)。execution.py 已迁移至消费本模块、删除私有公式，兑现决策1“两处不得各自维护”。
- **验证**：迁移后 `run_tests.py` 实测 `total=112, passed=112, skipped=0, failures=0, errors=0` 全绿；纯现金分红路径(茅台 fixture)参考价迁移前后为 `1184.08` 不变；纯送转路径 fixture 迁移前后为 `6.67` 不变。
- **已知局限**：切点固化为2023-02-17(当前规则文本发布日)，早于此的除权日在共享模块层fail-closed。Phase 1哑策略下execution.py不实际命中该边界；待未来真实持仓策略会持有配股标的、或execution.py需处理2023前除权日时，再外部取证公式历史生效版本、将切点退至回测窗口起点。

## 2026-07-03 | PIT复权服务可见性判定的asof口径修正
- 发现:PITAdjustmentService(968cb10)判定CA可见性时硬编码09:00 cutover asof,而非用服务自身的derivation_asof_ts。后果:盘后(≥15:00)derivation视角下,available_at=除权日15:00的迟到CA本已可见、因子本可构造,却被09:00口径误判为UNPROCESSED_BOUNDARY而误BLOCK,导致除权日复权因子系统性丢点(A股分红送转集中5-7月,误阻断成片)。
- 原因:同源架构下,可见性判定函数(evaluate_corporate_action_visibility)由handler与service共用是正确的;但两消费者必须传各自的asof——handler传09:00执行cutover(判开盘前能否处理),service传derivation_asof_ts(判盘后因子能否构造)。原实现让service错误复用了09:00口径,把"判定函数单源"误做成"asof也统一",违背同源本义(公式该合、asof该分)。
- 修复(f4c012e):service判定层三处改用derivation_asof_ts,删除service内09:00硬编码;共享判定函数将"ex_date当天"按APPLICATION_CUTOVER_TIME(09:00)细分——09:00前后分别落UNPROCESSED_BOUNDARY/NOT_YET_VISIBLE,使同一函数在handler(09:00)与service(盘中14:59/盘后15:00)三个asof下各自正确分叉。
- 验证:handler侧09:00对601318类迟到CA仍返回UNPROCESSED_BOUNDARY(改前改后逐点不变,手算+既有测试test_same_day_late_visible_ca_is_marked_unprocessed确认);service盘后该除权日为OK,daily_adjusted_return=9.50/9.00-1=+5.56%(手算);116测试全绿。
- 已知有界瑕疵:APPLICATION_CUTOVER_TIME=09:00作为常量写入共享判定函数,使执行层时间口径渗入本应中立的判定逻辑。当前行为正确、handler语义不变;列入Phase2收官审计,评估是否将cutover时刻完全外置由调用方asof自带。
- 镜像教训:与2026-07-03参考价双源合一对照——参考价是"两处公式必须合一",asof是"两处口径必须各传";同源的真义是判定逻辑单源、而非输入也统一。

## 2026-07-03 | PIT复权服务price_basis校验缺口修复
- 发现:LT-002C测试暴露PITAdjustmentService读daily_bar_raw时未请求/校验price_basis,vendor-adjusted(hfq/qfq)行会被静默当raw使用,在复权服务内叠加第二重复权,收益口径失真。
- 原因:Service读取路径漏了防泄露架构本应强制的price_basis校验(规范§3.4"默认拒绝price_basis缺失/非RAW的数据");firewall在此处未落地,靠测试才抓出。
- 修复(21aee51):_daily_bars()请求price_basis列,_require_columns要求其存在(缺失DataContractError),_assert_raw_unadjusted对None/NA/非RAW_UNADJUSTED一律抛DataContractError,复用既有错误类型不新造。RAW正常路径不受影响。
- 验证:LT-002C由FAILED转PASSED;全量121测试全绿(基线116+5)。
- 附:同批修正一处测试设计错误——原UNPROCESSED_BOUNDARY在Service层的直接补测构造了available_at>asof的迟到CA,被portal的available_at过滤正确挡掉、无法进入判定层,故该态在Service盘后asof语境下不可自然触发(它是handler 09:00 cutover层的态)。替换为"未来CA在当前asof不可见→走raw-close"测试(与LT-002B成对),BLOCKED传染在Service层由UNSUPPORTED_TYPE覆盖(968cb10已有)。
---

## 2026-07-05 | PIT复权服务三项健壮性修复(F1/C-CRITICAL/F4)
- 发现:全库审计深读暴露三处契约/统计缺口。F1:复权服务对"全日历面板"是隐含契约零强制,窗口内已可见CA的ex_date落在缺bar日会被静默跳过。C-CRITICAL:非CA缺行日用相邻可见close算收益,跨缺行的多日raw收益被当逐日收益(项目最高红线"跨除权日多日收益机械失真"的静默入口)。F4:bootstrap小样本(<=block=21)返回(mean,mean)零宽CI,把"样本不足"表达成"完全确定",方向反转。
- 修复:F1(7ccee46)窗口区间[首bar,末bar]内已可见CA的ex_date不在bars日期集则整窗BLOCKED(reason=CA_EX_DATE_ON_MISSING_BAR_DATE),范围限定防过度阻断。C-CRITICAL(初版e097d4c用weekday推断违反§1.1铁律被拦回,bea596a改用注入真实交易日历TradingCalendar判previous_trading_day相邻性,长假后首日正确判相邻不误BLOCK)。F4零宽CI改(None,None)+ci_method标注insufficient_samples。
- 验证:F1双向断言(缺行日CA阻断+窗口后CA不阻断)、C-CRITICAL跨长假首日OK哨兵、F4小样本(None,None)+大样本仍走bootstrap,均通过;144测试全绿。
- 教训:C-CRITICAL初版擅自用weekday skeleton绕过"缺calendar依赖",是"缺依赖时自行降级而非停下报告"的第四次同类;后续指令预置"缺依赖停下报告不得推断/近似绕过"。

## 2026-07-05 | 参考价切点从2023-02-17退至2011-02-28
- 发现:共享参考价规则模块切点设2023-02-17(当前规则文本发布日),但除权除息参考价公式在回测窗口全程存在,十年回测命中2017-06-01历史除权日时fail-closed抛错、回测崩溃。
- 原因(含审计者判断失误复盘):08f8c24固化切点时我已发现切点太激进会让2023前除权日报错,却听信"有界"论证(RIGHTS_ISSUE仅19条、哑策略几乎不触及)将其降级为"有界局限暂缓"。该论证范围错配:有界的是配股罕见,而切点fail-closed拦的是全部除权日(含最常见的现金分红/送转),触发面根本不有界。直到十年回测才引爆。
- 修复(eefb1c5):外部取证深交所《交易规则(2011年修订)》§4.4.2,实施日2011-02-28,公式[(前收盘-现金红利)+配股价×股份变动比例]/(1+股份变动比例)自2011版至2026版逐字未变(独立核实:会计视野法规库深证会[2011]5号实施2011-2-28、中证报投教专栏、2011/2023/2026三版公式比对,检索2026-07-05)。切点退至2011-02-28,之前保持fail-closed;公式不变只扩生效日覆盖,版本化保留。
- 验证:2017-06-01纯分红参考价9.50、2012-05-21配股10.00手算通过;十年回测跑通,nav_daily/order/fill/corporate_action四类账本哈希与f29d083基线逐字节相同(Phase 2未动摇Phase 1闭合输出),144测试全绿。
- 方法论留痕:凡标"暂缓/有界局限"的判断,必须写清豁免的确切边界,并标注"何时必须复检"(切点这颗雷正是缺复检点、记DECISIONS后当已解决);边界可能被触及的场景(如十年回测接入)前主动复检。

## 2026-07-05 | 标签生产计算器与未来泄露防线(7d4da15/af8cd44)
- 背景:LabelDataPortal原为读取器,黄金切片需从L1价格+L2台账真算T+1开盘→T+22开盘PIT复权收益标签。
- 关键设计:①标签复权唯一走PITAdjustmentService新增的open_to_open_adjusted_return,计算器不自读价/不自写复权(§3.3.4同源);②灵魂约束——标签derivation_asof固定取出场日T+22收盘后(15:00),与信号推理asof(T日)物理分离,站在T日无法算出标签;③label_observed_at=出场日收盘后(非开盘),防"出场日开盘当训练器已知"的半天泄露窗口。
- 防线证据:泄露堵截测试用T日asof(2026-01-02)返回算不出、正确性测试用T+22 asof(2026-02-03)才产出,两asof不同,泄露被架构堵死非手算自洽;补强测试钉死出场日盘中13:00标签仍未成熟;open_to_open含CA开盘窗口独立手算算例(参考价9.00/因子0.9/收益0.1)验证CA场景复权正确。
- 顺带:IC层补price_basis==PIT_DERIVED校验(审计C类),给已过审PITAdjustmentService加开盘方法带回归锁(收盘口径reference=9.00逐值不变,144零回归)。
- taint:全程EXPLORATORY_TAINTED,标签计算器解锁的是"能从真实价格算标签"的能力,不是数据可信度升级。

## 2026-07-22 | 巨潮披露时刻缺失取证与黄金切片available_at方案X
- 取证结论:巨潮历史公告接口的`announcementTime`对149条黄金切片待核验公告中147条仅保留日期级零点,仅`000333`的2条2024年近期公告保留秒级时刻;扫描12只票三通道共7995条原始公告记录,未发现`storageTime`、`pubTime`等可替代的精确披露时间字段。历史精确披露时刻从该接口不可得。
- 决定:黄金切片公司行动将`disclosure_date`与`disclosure_time_known`拆分。`disclosure_time_known=False`时,以真实`TradingCalendar`将`available_at`派生为披露日次一交易日09:30(Asia/Shanghai);若派生值`>= ex_date`,立即fail-closed抛`DataContractError`交人工核验,本阶段不建BLOCK降级机制。
- 单源约束:`resolve_ca_available_at`是唯一派生实现,核心`PITDataPortal`与`CachedPITDataPortal`共同调用,从结构上防止快慢路径分叉泄露。实现见`a8a2a71`;全库162测试全绿,十年回测order/fill/corporate_action/nav四类账本哈希逐字节不变,`601318`的2015年旧记录因缺少`disclosure_*`字段继续走旧`available_at`路径,行为不变。
- 待办③:面向全市场台账可信化时再建设不可验证可见性的优雅BLOCK传播机制,并补`disclosure_time_known=True`路径的`available_at >= ex_date`越界检查;本次黄金切片小样本由人工在场、越界直接报错。
- `601318`异常记录:2015-09-09记录满足`ex_date == announcement_date`,但`record_date=2015-09-08`早于`announcement_date`,业务时序不成立,强烈指向akshare的`announcement_date`口径错误。该判断仍属疑似,未经2015年官方PDF闭环确认;记录属于全市场`EXPLORATORY_TAINTED`台账,不进入黄金切片,不阻塞冻结。
- Verified absence:12只黄金切片证券在冻结窗口内`配股=0`、`转增=0`、`送股=0`;该切片为纯现金分红场景,EX-012拆股路径不触发。

## 2026-07-27 | 黄金切片CA台账人工核验完成并冻结(26f4ee4)
- 范围:12只票、窗口2020-01-01~2023-12-31,76条A股普通股CA记录(88行原始核验数据中,排除优先股11条、去重000333那对2021-05-26重复件1条)。
- 核验方式:逐份打开巨潮官方PDF原件,人工读取每股现金红利(税前含税)、除权息日、股权登记日;每条锚定`source_pdf_filename`与`source_pdf_sha256`,76/76哈希校验通过。`verified_by=George`,`evidence_level=MANUALLY_VERIFIED_OFFICIAL_PDF`。
- 两个金额字段的口径分离(重要):`cash_dividend_per_share`=实际派发给参与分配股本的每股金额(现金账本记应收用);`ex_right_cash_deduction_per_share`=按总股本折算后除权价实际扣减的每股金额(除权参考价计算用)。76条中23条有折算节、22条两字段数值有差异,最大差异000651/2021-08-14(3.0→2.784787,相对约7.2%)。两者混用会造成除权日价格调整偏差。
- `disclosure_time_known`统一为`False`:巨潮对这批历史公告仅提供日期级披露时点;000333在2021-05-26的更新后实施公告与更正公告虽带秒级时刻(07:49:57/58,相隔1秒),形似批量上传时间戳而非真实披露时刻,不足以据其标记`True`。此处同时更正2026-07-22条目中的“2024年近期公告”:实际日期为2021-05-26。全部走方案X次一交易日09:30保守派生;方案X边界预检76/76通过,派生可见日距除权日3-10个自然日。
- 依赖窗口覆盖:前置依赖日2018-12-19(由真实`TradingCalendar`自2020-01-02回溯252交易日得出;252为动量总跨度,非231评分窗,skip的21日价格参与计算);后置依赖日2024-01-31(最后信号交易日2023-12-29的T+22)。取证窗口经补抓后为2018-09-01~2024-03-31,共252份官方PDF。区间[2018-12-19, 2019-02-24]经全量标题扫描与除权日物理特征扫描,在已取证范围内未发现A股普通股除权事件。此结论表述为“在已取证范围内未发现”,不表述为“确认无遗漏”。
- 冻结:`selection_frozen_at=2026-07-27T23:19:33+08:00`,`manifest_hash=94e025b6a0b259c56751c6d3f3953c4a804aeea1c85b3b73dac9f9b2f468d4ae`,`selection_independent_of_strategy_results=true`,`assert_frozen_and_consistent`自检通过。冻结在任何策略结果之前完成(§0.5铁律),本步未运行任何信号/IC/回测。167测试全绿(基线162+5),新增测试含“篡改CA金额触发hash不匹配”。

## 2026-07-27 | 更正:送股“verified absence”结论有误,标题筛查不等于正文核验
- 取代此前记录的“配股0/转增0/送股0,黄金切片为纯现金分红,EX-012拆股路径不触发”。
- 事实:配股确为0条;但恒瑞医药600276有3条非零送股(每股送红股0.2股,除权日2019-03-28/2020-05-25/2021-06-10),其中后两条落在冻结窗口内。
- 错误成因:该“0”来自ch3全量标题的关键词族筛查,而送股信息在公告正文、标题只写“权益分派实施公告”,标题筛查捞不到。我把筛选结果当成了完整结论并表述为verified absence。
- 影响修正:参考价公式在恒瑞这3条上分母不为1,须用完整公式`[(P-D)+Q×r]/(1+s+r)`;本切片配股比例`r=0`,故实际为`(P-D)/(1+s)`。EX-012送转账本路径在黄金切片会触发,不是“不触发”。
- 纪律:任何“未发现X”的结论,必须写明其检出手段的覆盖范围(标题级/正文级/物理特征级),不得跨越手段边界升级为verified absence。标题扫描能保证“没有整份公告被漏”,不能保证“已抓公告的正文无未声明事件类型”。

## 2026-07-27 | 巨潮列表页标题与PDF正文标题可能不一致(全市场取证必读)
- 事实:中石化600028自2021年起,巨潮列表页标题为“XX年度A股股息分派实施公告”,而PDF正文第一页标题为“XX年度末期A股分红派息实施公告”。公司文件措辞未变,变的是巨潮列表页的标题字段。
- 后果:原取证关键词表含“分红派息”不含“股息分派”,导致600028自2021年起6条常规实施公告未进listing;另有2020-10-15“特殊股息分派实施公告”及其方案公告(2020-08-31)也一并遗漏。该次除权(每股0.07元特殊股息,出售油气管道资产)在补漏前于台账中完全不存在——不是数值错误,而是整个除权日缺失。
- 教训:按PDF正文措辞建关键词表永远抓不到这类差异,必须按列表页标题抓;关键词表须含“股息|分派”等同义措辞。查漏应采用扩展业务词全量标题扫描+已下载集合差集,并辅以正文物理特征扫描(同时出现除权(息)日+股权登记日)。
- 补漏结果:中石化补齐上述6条常规实施公告及特殊股息相关公告后,五年呈“年度+中期”整齐节奏,一年不缺;其余11只票经同法扫描未发现同类实施类遗漏(在已取证范围内)。

## 2026-07-27 | 更正:沪市实施公告同样存在除权折算节
- 更正此前“沪市公告不写除权公式、直接取每股现金红利”的工作判断。
- 事实:601318平安9条、600276恒瑞2条、600900长电1条为“沪市差异化折算”,公告中明确给出按总股本折算后的每股扣减额,与名义每股现金红利不同。
- 错误成因:从恒瑞3份无折算节的样本,推广为“沪市都没有折算节”。若按该判断执行,这12条会全部采用错误金额。
- 正确做法:不按交易所划分,逐份检查是否存在“除权除息价的计算原则及方式”一节;有则取公式中前收盘价减号后的折算值,无则取每股现金红利。深市与沪市均可能有。

## 2026-07-28 | 黄金切片块4a完成:端到端管线在冻结台账上跑通(e749a73)
- 范围:12票、信号日[2020-01-02, 2023-12-29]共970个交易日,逐日出信号;snapshot=golden_slice_2026-07-28_ADJUSTMENT_ONLY(L1 15,516行、CA 76行,source_snapshot_id保留为20260630_akshare_raw)。冻结门禁在运行入口通过,manifest_hash=94e025b6...。
- 复权链路自检结果:方案X路径76/76经disclosure_time_known=False派生(抽样含601939 2023-07-08周六→2023-07-10周一09:30,证实走真实TradingCalendar非自然日+1);全窗口复权点OK=15,470 / NO_DATA=46 / BLOCKED=0,NO_DATA原因均为MISSING_CLOSE_OR_PREVIOUS_CLOSE(窗口边缘缺前收盘),占比0.3%。
- 组合事件双字段消费验证:恒瑞600276 2020-05-25(现金+送股组合),前收盘95.88、现金扣减0.23、送股分母1.2,手算参考价79.70833333333333333333333333与服务AdjustedReturnPoint.reference_price逐位一致。证明action_type=STOCK_DIVIDEND未导致现金字段被忽略。
- 标签:OK=11,587 / NO_DATA=31 / 入场不可交易11 / 出场不可交易11 / 未成熟剔除0。停牌三段处理在真实数据上生效。
- IC结果(仅为管线诊断,不构成策略有效性证据):RankIC均值0.1093、ICIR 0.2490、block bootstrap CI [0.00518, 0.19765]、分位收益0.008026/0.013847/-0.001225/-0.004864/-0.002794、单调性NOT_MONOTONIC。
- 【对IC的明确判定,防未来误读】该RankIC不得被援引为动量策略有效的证据,理由:①横截面每日仅约11.2只票、分5档每档约2.2只,单只证券即可翻转分位;②分位收益非单调(第二档高于第一档、后三档挤在零附近),形态更符合“少数证券贡献全部相关性”而非真实截面alpha;③block bootstrap CI在n≈12的截面样本上本身不可靠,CI不跨零在此不构成显著性证据。黄金切片证明的是管线诚实,不是alpha存在。
- 状态三元组:evidence_status=EXPLORATORY_TAINTED(符合§12.3)、audit_status=PENDING_AUDIT、validation_scope=GOLDEN_SLICE_PIPELINE、validation_scope_manifest_hash=冻结hash。本步不自我发证。
- 运行成本记录:标准PITDataPortal逐日全链路在健康计算状态下运行至墙钟6164.9秒仍未完成(人工中断,CPU 6094.7秒);改用CachedPITDataPortal完整跑完墙钟4146.7秒(含一次宿主暂停)。因标准路径未跑完,不推导性能倍数。Portal切换的等价性由抽样验证保障:600276在2020-05-11~2020-06-05共20个交易日(含除权日)上,两条Portal逐点status/reference_price/adjusted_return/raw_close/ca_on_date/block_reason及序列元数据完全一致。
- 【Phase 3成本提示】标准Portal逐日重读parquet+逐点契约校验的成本,在全市场规模下不可接受;Phase 3的实验循环须默认走缓存路径,并沿用快慢等价哨兵保障语义一致。

## 2026-07-28 | akshare台账缺口实证:茅台两次特别分红缺失
- 事实:76条人工核验台账与akshare L2台账按(security_id, ex_date)双向差集,结果为——akshare有而人工台账无:0条;人工台账有而akshare无:2条,均为600519贵州茅台的特别分红(ex_date 2022-12-27每股21.91、2023-12-20每股19.106)。
- 含义:①人工核验未漏任何akshare已记录的事件(0条方向的空集);②akshare台账确实存在缺口,且缺的是金额显著的特别分红——茅台这两次每股派息量级与年度分红相当,遗漏会使跨该除权日的收益严重失真。
- 这与此前中石化6条(列表页标题措辞差异导致取证遗漏)是不同性质的缺口:中石化是我方取证关键词不全,茅台这2条是数据源本身未收录。
- 全市场台账可信化时,须假定akshare对特别分红类事件存在系统性遗漏,不得以“台账无记录”推断“无事件”。

## 2026-07-28 | 黄金切片块4b完成:执行层与CA账本在人工核验台账上验证(a0f918d)
- 前置修复:f30cd6c修正执行层双字段路由——execution.py的除权参考价改读ex_right_cash_deduction_per_share(折算扣减值)、缺列/None/NaN时回退cash_dividend_per_share;corporate_action_handler.py不改,现金账本继续消费cash_dividend_per_share(实际派发值)。十年四类账本哈希逐字节不变(3075条老台账无新列全走回退),172测试全绿。
- snapshot:golden_slice_2026-07-28_EXECUTION,三表(L1 15,516行 / CA 76行 / security_master 12行)。双字段断言:22条两值不同、54条相同;最大差异000651 ex_date=2021-08-23(实际派发3.0 vs 折算2.784787)。
- 信号复用:物理列投影仅加载signal_asof_ts/score_asof_ts/security_id/signal_status/momentum_score/cross_sectional_rank,未来收益、label、RankIC字段物理未加载。校验口径为ordered hash binding(970交易日与feature manifest的970个hash按序绑定),【非内容密码学校验】——predictions.parquet无hash列、feature manifest的hash数组无日期键、momentum_score写入时Decimal转float有损,无法无损重建原始hash载荷。
- 执行:月度调仓(真实交易日历每月首个交易日,共48日)、等权top_n=3、初始资金1,000,000元。订单122、成交91、拒单31(全部CASH_INSUFFICIENT)、lot size调整0、总费用9978.63。净值恒等式最大偏差0.00。
- 【双字段路由的最终兑现】确定性账本审计(不依赖策略碰巧持有,用预置持仓强制覆盖):000651/2021-08-23手算与账本均为1000×3.0=3000.00,【未使用折算值2.784787】;000651/2022-04-08为1000.00;000333/2023-06-01为2500.00。三条手算与账本逐值一致。这是人工核验发现"两个金额口径"→冻结台账双字段→执行层分流修复→真实回测正确取值的完整闭环。
- 送股账本(恒瑞两次,ex_date 2020-05-25与2021-06-10;2019-03-28在回测起点前):share_delta=int(1000×0.2)=200股、总成本基10000.00前后不变、cost_basis_delta=0.00、每股成本10.00→8.333333(=10000/1200)、新股sellable_from=ex_date立即解锁。该语义与现有portfolio_ledger实现一致。
- CA账本:主回测自然触发27条(分红应收13、简化到账13、送股1),UNPROCESSED_CA_count=0。
- 【本步未验证的边界,不得被后续引用为已验证】
  ① 执行路径覆盖不全:31条拒单全为CASH_INSUFFICIENT,涨跌停拒单、停牌不成交、容量截断、缺开盘价、lot size调整在本次回测中【一次未触发】。未触发≠已验证,这些路径在黄金切片上无实证。
  ② broker_profile缺失:仓库无broker_profile实现,本步使用Phase 1默认FeeSchedule测试口径。§12.3第6条(成交、成本、容量、公司行动与未成交订单纳入净值)【未完全满足】。
  ③ 派息支付时序:现金到账采用既有"除权后次一交易日"简化(corporate_action_handler实现),非官方发放日。本步仅验证应收金额路由,支付时序未验证。
  ④ 历史ST状态:security_master的is_st/status为CURRENT_SNAPSHOT_ONLY污染字段,不能证明12票在2020-2023全程非ST。"窗口内非ST"是冻结选样假设(选样以超大盘蓝筹、历史未被风险警示为标准),【未经PIT ST数据验证】;历史ST涨跌停切换本轮不验证。
  ⑤ 输入数据洁净度:L1价格仍来自akshare_raw快照,仅CA台账经人工核验。准确表述为"在人工核验CA台账上的执行层验证",【不得称全部输入数据已干净】。
- 状态三元组:evidence_status=EXPLORATORY_TAINTED、audit_status=PENDING_AUDIT、validation_scope=GOLDEN_SLICE_PIPELINE、validation_scope_manifest_hash=冻结hash。不产出任何业绩结论(未计算年化/夏普/回撤/胜率,未生成净值曲线)。
- 测试172→178全绿。核心执行/账本/CA handler、冻结manifest、4a工件均未改动。

## 2026-07-28 | 更正:分位单调性结论是分组余数规则的产物,非数据事实(取代 2026-07-28 块4a留痕中"IC不得援引为alpha证据"的第②条理由)
- 被取代的表述:此前以"分位收益非单调(第二档高于第一档、后三档挤在零附近)"作为"RankIC不得援引为alpha证据"的理由之一。
- 事实:momentum_ic_evaluation.py:204-206 的分位分组为 `quantile = min(5, (index*5//n)+1)`,桶大小 = ceil(nb/5)-ceil(n(b-1)/5),余数恒分配给最靠前的桶。日频截面数分布 {11:614天, 12:257天, 10:99天} → q1 在 871/970 天里有3只、q5 恒2只。独立复算证实:换用"余数给中间桶"这一同样合理的分配规则,q1均值 0.008026→0.011035(变动+37%)、q2 0.013847→0.009275,顶端反转消失、前三档单调递减。
- 因此"非单调"是实现产物,不是数据事实,不能作为理由。
- 【替换后的正确理由】横截面仅10-12只证券而分5档,分位数在统计上没有良定义,任何单调性判定都不稳健。这比"观察到非单调"更根本,且不依赖分组实现。
- 结论方向不变:RankIC 0.1093 仍不得援引为策略有效性证据。理由重述为:①截面样本极小(每日约11.2只),单只证券即可翻转分位;②分位数在该样本规模下无良定义;③block bootstrap CI 对 block_length 完全敏感(block=21时[0.0052,0.1976]不跨零,block=42/63/126 均跨零;Newey-West同向:L=21 t=2.09、L=63 t=1.84),CI不跨零不构成显著性证据;④幸存者偏差未除,12票选自当前沪深300成分(非时点成分)。
- 方法论:本条是"把实现产物当成数据事实"的一次实例。任何写入DECISIONS的观察结论,须先确认它不是分组/舍入/排序等实现细节的副产品。

## 2026-07-28 | 更正:转增=0 的结论从未复检,降级为未验证(取代 2026-07-22 条中的"转增=0")
- 2026-07-22 记录的"配股=0、转增=0、送股=0"三个结论出自同一次标题关键词族筛查。2026-07-27 的更正条只推翻了"送股=0"(恒瑞3条),并断言"配股确为0",但**从未复检"转增=0"**。
- 三者检出手段相同(标题筛查)、失效理由相同(送转信息写在正文,标题捞不到),因此"转增=0"与已被推翻的"送股=0"具有同等的不可靠性。
- 现状:ca_verified_88.csv 表头无"转增"字段;77行A股普通股中74行"送股比例_每股"为空(空=未填写,代码当0)。**人工核验层没有任何字段能区分"核验为零"与"未检查"。**
- 本条不断言转增为零或非零,仅将该结论降级为"未验证"。待办:①按76条对应PDF复检转增(与送股同法)②核验表增加"已核验为零/未检查"的区分字段。
- 这是 2026-07-27 所立纪律("任何'未发现X'的结论必须写明检出手段覆盖范围")的一次现行违例,由本次审计检出。

## 2026-07-28 | 修复过户费生效日与配股字段单源化(M-04/M-03),十年账本基线作废重建
- 【M-04 过户费生效日】execution.py 的过户费下调切点由 date(2025,4,29) 更正为 date(2022,4,29),费率0.00001不变。外部依据:中国结算自2022年4月29日起将股票交易过户费由沪深A股按成交金额0.02‰双向收取统一下调为0.01‰(人民网、中国日报网,检索日2026-07-28)。原值使 2022-04-29 至 2025-04-28 近三年每笔成交的过户费为真实值的2倍。
- 【测试固化错值的教训】原 test_execution.py 用 2025-04-28/2025-04-29 断言费率切换,是顺着实现写的断言、无出处,把错误日期钉成"有测试保护的正确行为"。这与2026-07-01印花税bug是同一形态,且活过了两轮全库审计。修复后测试改为断言 2022-04-28/2022-04-29 边界并在注释中附外部URL,另新增一条断言 2025-04-29 不再是切点,防止旧值以任何形式复活。
- 【1900-01-01 占位未修,已标注】transfer_fee_rates 仍以 date(1900,1,1) 为起点,该起点无官方依据。经查上交所/深交所口径:2015-08-01 前深市按成交金额0.0255‰收取(不是"不收"),沪市按成交面额计收,统一为按成交金额0.02‰自2015-08-01起施行。十年回测中 2015-07-01~07-31 有14笔成交(过户费0.71元)落在该区间,数值不可靠。本批未修,待办已记入STATE(触发条件:回测起点早于2015-08-01时)。
- 【M-03 配股字段名不匹配与单源化】生产 corporate_actions.parquet 的列名为 share_ratio 与 rights_price_per_share,不存在 rights_ratio / rights_price。execution.py 原用 getattr(row,"rights_ratio",0)/getattr(row,"rights_price",0) 直读不存在的列恒得0,且RIGHTS_ISSUE分支continue掉连share_ratio都不累加 → 19条配股的除权日限价参考价等于前收盘,即在真实除权日声称"当日无公司行动"。而 pit_adjustment_service 对同一份数据做了完整schema适配——同语义两份实现且已分叉。
- 修法:抽出唯一共享函数 src/domain/corporate_action_inputs.py::extract_rights_issue_terms(rights_ratio→回退share_ratio、rights_price→回退rights_price_per_share、比例为正而价缺失则raise DataContractError),execution 与 PIT 服务两处消费者均调用之,PIT 服务原私有适配 _rights_price/_optional_field 已删除。生产适配器确认 RIGHTS_ISSUE.share_ratio 来自"配股方案"、无独立送转含义,故保留 continue 正确、避免重复计入分母。
- 【M-03 在十年回测中零触发】19条 RIGHTS_ISSUE 的 ex_date 与订单决策日、成交日重合数为0,故该修复未单独引起任何输出变化。审计估算的5.2%参考价偏差是理论量级,在本回测中从未实际发生。
- 【新基线,取代 f29d083 逐字节一致】旧基线记录的是带M-04错误的输出,自本次修复起作废。新四类账本哈希:
  nav_daily 78c5310cc226820effc81cc542531461826ab4f19683dccdf9bd1c34fa8f44c7
  order_ledger bcabb3855581cc96765853546aeee4d74b9b31b8abfe82936d4e382831694554
  fill_ledger 67950491a11bdc9b8bdbba98fd4637df2d3033df469989baaf1777f81683d95c
  corporate_action_ledger 49dee0cd7c1231ddb081342491852b5a57e90eac2a73a69d42076a1e0e2de1fe
  十年:trading_days 2671、订单168、Fill 211、成交74、CA 245、UNPROCESSED_CA 0 全部不变;total_fees 476.75→476.73(-0.02);final_nav 1,031,906.54→1,031,906.56(+0.02)。
- 【归因验证,新基线的含金量高于旧基线】旧基线只证明"可复现",新基线经逐项归因:①2022-04-29后仅2笔成交(000425 2025-01-15/2025-01-22)各由0.02→0.01,切点前72笔合计4.42一分未变 ②CA账本245条中34条变化,唯一变化字段为 available_cash_after(累计+0.02),事件类型/日期/数量/现金delta/成本基delta/已实现PnL/持仓/reason 全部零变化 ③旧费率进程内重放精确复现四个旧哈希,证明比较口径一致。所有业务差异完全归因于过户费费率修正,无第四处变化。
- 【判据订正与方法论】第三段验收原设"CA账本哈希必须不变",该判据错误——PortfolioLedgerEntry 将累计现金状态 available_cash_after 写入每条账本条目,费用变化必然平移后续所有条目。正确判据为"CA的事件类型/日期/数量/业务金额不变,累计状态字段允许随现金平移"。Codex 严格按错误判据停机并把根因查到字段级,行为正确。教训:验收判据可能设错,但 fail-closed 的执行不能松——误停的代价远小于漏过;若执行方自行判断"应该没问题"继续跑,我们就不会发现账本条目携带累计现金状态这一事实。
- 【4b重跑】total_fees 9978.63→9909.96(-68.67);41笔切点后成交过户费 137.27→68.60。订单122/成交91/拒单31(全CASH_INSUFFICIENT)/CA应收13、到账13、送股1/UNPROCESSED_CA 0/净值恒等式最大偏差0.00,业务字段逐笔不变。前一版4b工件已作废,新report中已标注。
- 【68.63 vs 68.67 的口径差】事前按总额估算为68.63(6,862,186.00×0.00001=68.62186),实际逐笔按分四舍五入后合计68.67。差异来自逐笔舍入而非估算错误,已查清,不作为噪声处理。
- 测试 178→185(净增7:生产字段回退、canonical字段优先、缺字段fail-closed、execution/PIT两条接线、旧2025切点防回归)。

## 2026-07-29 | X-04 同日多笔现金分红取证闭环:三组均为"一份公告合并实施两笔",非重复记录
- 【问题】生产 L2 台账存在 3 组同 (security_id, ex_date, action_type) 的现金分红记录,execution.py:1011、corporate_action_handler.py:255、pit_adjustment_service.py:655 三处均用 drop_duplicates(keep="last") 静默丢弃其中一条。
- 【取证】通过巨潮官方通道(ch1|ch2|ch3 全部命中,未使用备用直链)抓取三份官方实施公告 PDF 至 data/x04_evidence/(不入git),sha256 与字节数记于 x04_manifest.csv:
  300760_2025-05-22_1.pdf sha256=98470e8000f0d2c45c4b2584be0e92bb2741668cdba0a358858f8120c37d4fb9 (301,257 bytes)
  300760_2026-05-20_1.pdf sha256=c13e4f6fd029b359dfb349c7d370fcaa9a4bc3a59791a17c24c07f4539267457 (120,876 bytes)
  301308_2026-05-26_1.pdf sha256=25ec59eab647f69b51f34936fef2a23f937f5d86c081f4b9d25e983a4f8c8a40 (131,415 bytes)
- 【人工核验结论:三组全部为"两笔可加",无一组是重复记录或修订件】
  ① 300760 迈瑞医疗 / ex_date 2025-05-29:《2024年度及2025年第一次中期权益分派实施公告》,原文"每10股派发现金红利19.70元(含税),由2024年度利润分配方案每10股派发5.60元与2025年第一次中期利润分配方案每10股派发14.10元共同构成"。无折算节,总股本 1,212,441,394 股全程未变,名义=实扣。
  ② 300760 迈瑞医疗 / ex_date 2026-05-28:《2025年度及2026年第一次中期权益分派实施公告》,句式与①一字不差,合计15.60元 = 3.10元 + 12.50元。无折算节。
  ③ 301308 江波龙 / ex_date 2026-06-02:《2025年度、2026年第一次中期权益分派实施公告》(公告编号2026-065),原方案 2025年度每10股3.50元 + 2026年第一次中期每10股6.50元 = 合计10.00元;有折算节。
- 【③ 的折算机制:股权激励归属导致股本增加,非回购】初查曾推断为"回购股份不参与分配",读原件后纠正:2026-05-18 公司2023年限制性股票激励计划归属登记 3,915,740 股上市流通,股本总额由 419,145,267 股增至 423,061,007 股;调整原则为"分配总额不变",分红总额锁定 419,145,267.00 元,故每股摊薄。折算原文:每10股派发现金红利=现金分红总额/总股本*10=419,145,267.00元/423,061,007股*10=9.907442元。
  与已知的"回购股份不参与分配"型折算(如美的、格力)方向相反:回购型是分母减小(参与分配股本少于总股本),股权激励型是分母增大(总股本变多)。建全市场台账时须区分两种折算机制,不可混为一谈。
- 【警示:1:1 数值巧合,解析脚本极易误判】③ 中现金分红总额 419,145,267.00 元与调整前总股本 419,145,267 股数值完全相同(因原方案恰为 1.00 元/股)。任何从公告文本解析折算公式的脚本都可能把分子误认为股本数。将来若实现折算解析,须专门断言分子单位为元、分母单位为股。
- 【口径:截尾非四舍五入】③ 公告明写"结果取小数点后六位,最后一位直接截取,不四舍五入"。真值 419145267/423061007*10 = 9.9074425...,截尾得 9.907442,四舍五入会得 9.907443。逐位比对的回归断言须用截尾。
- 【拆行口径决定:合计自洽优先】③ 拆两行时,税前列按公告原始数字 0.35 / 0.65;实扣列按"合计自洽"分配为 0.346760 / 0.643984,使两行之和严格等于公告合计 0.990744。备选"行内自洽"(各笔按其分红总额独立截尾,第二笔得 0.643983)未采用。依据:除权按合计一次性扣减(前收盘−0.990744),拆行仅为记录来源期间;若两行之和≠公告合计,"两行相加==公告合计"这条最自然的守门断言将失效,而该断言正是防止将来拆行拆错的防线。末位差归入最后一笔。
- 【按比例分摊是错误路径,须按各自总额独立除】初查用 0.35×0.9907442=0.346760 验算,数值正确但路径错。正确算法是各笔按其分红总额独立除以同一总股本:146,700,843.45/423,061,007=0.346760…、272,444,423.55/423,061,007=0.643983…。两条路径在本例等价仅因分母相同;若两笔分红的基准股本不同(如中期方案基准日在股本变动之后),按比例分摊必错。
- 【修法定案:fail-closed + 人工确认白名单,不改去重键】同键多行改为 raise DataContractError,三组经本次取证确认者写入白名单(附公告 sha256)。不采用"去重键加分红所属期间"方案,理由:
  ① 台账现无"所属期间"字段,从 action_description 解析属文本依赖,与批次2 刚完成的"提取逻辑上收单源"方向相悖;
  ② 【更根本的边界】本类问题能被发现,仅因 akshare 恰好把合并实施拆成了两行。若数据源对"一份公告两笔分红"只返回合并后的一行,台账中不会出现同键,问题将以"金额偏小"的形式静默存在,任何去重规则都检不出。改去重键会造成"已修复"的错觉。当前对该情形无检出手段,记为已知边界。
  ③ fail-closed 与项目既有处置一致(方案X边界断言、NaT-CA、UNSUPPORTED_CORPORATE_EVENT)。
- 【影响量级】按 (证券,除权日) 去重,三组各丢一笔,丢失比例 28%(迈瑞2025:0.56/1.97)至 65%(江波龙:0.643984/0.990744),显著高于此前已修的格力折算差 7.2%。三组全在 2025-2026,黄金切片(76条,零同键)不受影响。
- 【本条仅为取证与定案留痕,修法实现未做】待办:实现 fail-closed + 白名单,并重跑十年回测(三组在回测窗口内,账本基线将再次变化)。
