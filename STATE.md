# 当前状态 (STATE.md)

> 每次工作会话结束时更新本文档。读完 = 立刻知道项目停在哪、下一步干什么。

---

## 当前阶段

Phase 2 收官审计已完成(GitHub远端审计+本地Cowork审计各一次,共识发现已合并),当前处于审计后修复阶段:批次1文档修正已完成,批次2(过户费M-04+配股字段M-03)待执行。

测试状态:178个test函数;干净环境(无akshare)实跑 total=175/passed=162/skipped=12,skip来源为TradingCalendar全类+L1数据契约+security_master契约(因文件顶部import akshare而全类跳过)。

---

## 已完成(Phase 2,接续Phase 1毕业f29d083)
- [x] 第一步 PIT复权服务:参考价模块(08f8c24)、组合测试(a430796)、execution迁移兑现决策1(6c044b5)、PITAdjustmentService本体+可见性同源(968cb10)、asof口径修正(f4c012e)、price_basis+五组哨兵(21aee51)
- [x] 第二步 截面动量:信号本体(ec53288)、IC评估+LabelDataPortal隔离(1d9a177)、信号驱动策略接执行边界执行层零改动(1b1affc)
- [x] 全库审计尾款:F1/C-CRITICAL/F4三项健壮性修复(7ccee46/bea596a)、参考价切点退至2011-02-28(eefb1c5)
- [x] 全库审计通过:克隆级技术深审+操作层清册+十年回测回归(四类账本哈希与f29d083逐字节一致,Phase 2未动摇Phase 1地基);Phase 2前两步审计结论保持有效
- [x] 黄金切片块1-3收官:标签计算器、manifest治理、方案X available_at、官方PDF人工核验与76条CA台账冻结均已完成(26f4ee4)

---

## 进行中

- [ ] 审计后修复批次2:过户费M-04+配股字段M-03待执行。

---

## 下一步

①审计后修复批次2(过户费M-04+配股字段M-03) ②其余审计发现按各自触发条件处置 ③审计缺口闭合后按§12.3十条件判定黄金切片能否标BACKTEST_VALIDATED ④Phase 3路线选择(全市场PIT数据能力为前置)。

---

## 收官审计待验清单(Phase 2已核验通过,存档)
- 六条待验清单+隐含契约扫描已于全库审计逐条核验;十年账本哈希逐字节一致
- 遗留记录级C类(黄金切片前或按需处置):标签计算器、NaT-CA丢弃、下游price_basis校验、bar去重、signal.asof_ts时间key规范化、label去重键

---

## 未决问题 / 阻塞

- 黄金切片执行路径覆盖不全:涨跌停拒单、停牌不成交、容量截断、lot size调整在块4b中均未触发,未触发不等于已验证。
- broker_profile尚未实现,§12.3第6条未满足;块4b仅使用Phase 1默认FeeSchedule测试口径。
- Codex会话沙箱对AppData下Python基解释器的访问权限已反复失效三次,均为权限误报而非文件丢失;需确认沙箱配置。
- 执行层 Windows 环境尚未确定（本机 / VM / 云）
- 券商档位待定，取决于计划投入本金
- 历史时点 ST 状态仍需更完整 PIT 数据源补齐（如 Tushare）
- 全市场CA台账可信化待办:建设不可验证可见性的优雅BLOCK传播机制,并补`disclosure_time_known=True`路径的`available_at >= ex_date`越界检查。
- `601318`的2015-09-09记录疑似`announcement_date`口径错误(`record_date`早于`announcement_date`),尚未经2015年官方PDF闭环确认;不进入黄金切片。
- 沪深主板ST/*ST涨跌幅限制自2026-07-06起由±5%调整为±10%。当前仅完成web多源确认:沪深北交易所于2026-04-24同步修订交易规则,其中该项变化适用于沪深主板;尚缺交易所公告文号的一手直引。黄金切片无ST且窗口不跨该日,不触发;全市场回测跨该日时须按生效日切换涨跌停判定。
- 标准`PITDataPortal`在日频全链路下的性能成本(块4a实测未跑完);Phase 3实验循环须默认缓存路径。
- X-04 同日多笔现金分红被静默去重丢弃(3组:`300760/2025-05-29`、`300760/2026-05-28`、`301308/2026-06-02`)。修法未定(相加 vs fail-closed报错),需先人工核这3组官方实施公告闭环。触发条件:回测窗口覆盖2025-05之后或全市场化。
- M-05 ST/*ST涨跌幅规则在引擎中从未实现(`LimitRuleTable`只按board解析,无ST分支)。此前STATE表述为"2026-07-06规则由±5%改±10%待切换",掩盖了"任何历史时期都未实现"这一更基础事实。触发条件:全市场回测纳入ST标的前——与PIT ST数据源并列为硬前置。
- M-01 CA在除权日不可见时复权服务静默产出未除权收益率;`corporate_action_availability`路径A/C无`available_at < ex_date`守卫(生产L2 3075/3075行走无守卫路径)。触发条件:引入真实公告时间戳(走路径A)前,或数据源列名变化时。
- D-02 volume单位跨源不一致(sina源=股、EM源=手,实测比值1.000151 vs 100.078534),永久哨兵固定抽3只sina股故恒绿。触发条件:任何换手率/参与率/ILLIQ/股数级容量约束上线前;同时哨兵须改为跨源抽样。
- X-01/X-02/X-03 CA输入提取同语义两份实现且已分叉(execution vs pit_adjustment_service),4a/4b快照对`cash_dividend_per_share`字段名重载仅由加载器边界保护。触发条件:全市场化前须上收至`src/domain`统一。
- G-01 冻结门禁在snapshot复用路径上不校验内容(只校验行数/列/id,不比对CA金额与manifest)。触发条件:任何"从冻结工件一键复现"的声称前。
- 转增=0未复检(见DECISIONS 2026-07-28更正条)。触发条件:黄金切片结论对外引用前。

---

## 备注

- GitHub 国内访问需要代理；如不稳定可考虑 Gitee 镜像备份。
- 本仓库根目录 = 项目的家，所有命令在带 `.git` 的这一层执行。
