# A 股量化交易系统 —— 项目宪法 (PROJECT.md)

> **唯一事实来源。** 每次开新对话 / 新 Claude Code 会话前，先读本文件 + `STATE.md`。
> 本文件描述"当前什么是真的"：目标、架构、约定。结构性变化时**原地修改**，使其永远是最新真相。

---

## 1. 项目目标

- 搭建一个**合规、可自动化**的 A 股中低频量化交易系统：
  数据获取 → 信号生成 → 回测验证 → dashboard/监控 →（券商通道）实盘执行。
- 明确预期：本项目不保证盈利；目标是做出一个**工程可靠、可迭代、风险可控**的系统，盈利取决于策略本身。

**当前阶段：Phase 1 — 事件驱动引擎(轨道A)**

---

## 2. 系统架构（两区，边界清晰）

```
[ 自建区 —— 完全自己掌控，不碰券商 ]            [ 券商区 ]
数据获取 → 清洗/口径对齐 → 特征/因子 → 模型/信号     miniQMT (xttrader)
   → 回测引擎 → dashboard/监控  ──── 信号 ────→        → 券商 → 交易所
                                                  ↑ 唯一绕不开的一环，仅作"下单 API 层"
```

| 模块 | 区 | 工具 |
|---|---|---|
| 数据获取 | 自建 | akshare / Tushare Pro |
| 清洗 + 口径对齐 | 自建 | pandas / polars |
| 存储 | 自建 | parquet / duckdb（起步够） |
| 特征 / 因子 | 自建 | 自写 |
| 模型 / 信号 | 自建 | ML 栈（alpha 不可 vibe-code） |
| 回测引擎 | 自建 | backtrader 或自建事件驱动 |
| dashboard / 监控 | 自建 | Streamlit 起步 |
| **执行下单** | **券商** | **miniQMT `xttrader`（xtquant）** |
| 风控 / kill switch | 跨区 | 自写 |

---

## 3. Gate 决策（卡在所有事前面，先定）

> 拿不准的先用推荐默认值占位，后续可在 `DECISIONS.md` 追加变更。

| 项 | 当前值 | 说明 |
|---|---|---|
| 券商档位 | 【待确认】推荐：低门槛起步（如国金 ~10 万档） | = 资金门槛 = 项目总闸 |
| 研究层部署 | 【待确认】如：本机 | 数据/模型/回测/dashboard，放哪都行 |
| 执行层部署 | 【待确认】需常开 Windows | QMT 仅 Windows；本机/VM/云 Windows |
| 数据源 | akshare（免费验证）+ Tushare Pro | 中低频够用 |
| 复权口径 | **等比复权** | 与 QMT 实盘一致，防 mismatch |
| 财务数据 | point-in-time | 防未来函数 |
| 策略频率 | 中低频（日频~分钟频） | 不触高频监管、数据自取 |

---

## 4. 关键约定（全链路统一，不得违反）

- **复权**：等比复权，研究与实盘一致。
- **时区**：Asia/Shanghai。
- **凭证安全**：券商账号、API key 一律走 `.env`，由 `.gitignore` 排除，**绝不进 git**。
- **单一 coding agent**：主力 Claude Code，不让两个 agent 同时改同一仓库。
- **数据口径对齐**：研究数据与 QMT 实盘行情口径必须一致，搭框架时即锁定。

---

## 5. 技术栈

- 语言：Python
- 数据：akshare / Tushare Pro
- 存储：parquet / duckdb
- 回测：backtrader 或自建事件驱动
- dashboard：Streamlit（起步）
- 执行：xtquant（`xtdata` 取行情 / `xttrader` 下单）

---

## 6. 协作工作流（核心机制）

**对话是一次性的，仓库是永久的。** 状态全部落在本仓库的三份活文档里，不依赖任何单次对话的上下文。

**每次工作会话的固定动作：**

- **开场（读）**：让 Claude Code "先读 PROJECT.md 和 STATE.md"；在 Claude.ai 网页则贴相关段落给它。把状态加载进当前上下文。
- **收尾（写）**：更新 `STATE.md`（进展 + 下一步）；有新决策则在 `DECISIONS.md` 追加一条；然后 commit + push。

**工具分工：**

- Claude.ai 网页：定方向、查文献、review 思路。
- Claude Code（VS Code / 终端）：写代码、跑回测、读写仓库文件。
- 仓库（git + GitHub）：唯一事实来源。

**指令规范（铁律，全程遵守）：**

对 Claude Code 下达的**所有**指令（写代码、改文档、git 操作、跑测试等）必须明确，不留歧义。每条指令尽量说清：

1. **具体动作**：做什么、用什么方式。
2. **涉及文件 / 范围**：动哪些文件，并在必要时圈定"别动其他文件"。
3. **需遵守的约定**：如复权用等比复权、先读 STATE.md 再开始。
4. **可验证的完成标准**：结果如何检查（如"跑通后报告拉了几条数据"）。

模糊指令是 agent 出错与自作主张的主要来源；明确指令让结果可预期、可检查。

---

## 7. 文档维护规则

| 文件 | 变化速度 | 更新方式 |
|---|---|---|
| `STATE.md` | 极快，每次会话 | 随意覆盖，旧内容不留 |
| `PROJECT.md` | 慢，结构性变化才动 | 原地修改，保持最新真相 |
| `DECISIONS.md` | 只增不减 | **只追加，永不删改旧条目** |

- 推翻旧决策时：在 `DECISIONS.md` **新写一条"取代 X"**，不删旧条目。
- 当某份文档开始服务两种变化速度/两类读者时再拆分（如未来的 `DATA_SCHEMA.md` / `RUNBOOK.md` / `RISK.md`），现在不提前造。
- **警惕**：过时的文档比没有文档更糟，会误导新会话。纪律核心：开场读、收尾写。
